# 東寧物語 (Tungning Story)

一款基於 Laravel 12.x + Livewire 開發的明鄭時期大航海背景網頁遊戲。

## 📜 專案簡介
《東寧物語》是一個結合角色扮演與模擬經營的 Web Game。玩家將扮演明鄭時期的開拓者，在台灣進行開墾、貿易與冒險。

## 🛠️ 技術棧
- **Backend**: Laravel 12.x
- **Frontend**: Livewire 3, Volt, TailwindCSS, DaisyUI (Retro Theme)
- **Database**: MySQL / SQLite (Dev)
- **Task Scheduling**: Laravel Scheduler
- **Localization**: 繁體中文 (zh_TW)

## 🌟 目前已實作功能

### 1. 人物與頭像系統 (Avatar System)
- **後台管理**:
  - 管理員可於後台「人物圖鑑」上傳、刪除頭像。
  - 支援批次匯入：透過 `AvatarSeeder` 自動讀取 `public/images/avatar` 內的圖片入庫。
- **前台選擇**:
  - 玩家可在「個人卷宗」頁面自由更換頭像。
  - 視覺化的頭像選擇介面。

### 2. 個人卷宗 (Profile)
- **介面重製**: 將原本的設定頁面改為古風「個人卷宗」樣式。
- **功能整合**: 整合頭像更換、基本資料修改、密碼變更與帳號刪除。

### 3. 權限與身分系統
- **多重身分**: Player (一般玩家), GM (巡查官), OM (運營總督)。
- **動態導覽列**: 依據身分切換前台/後台入口，支援官印 (Logo) 動態更新。

### 4. 遊戲參數與系統設定
- **府庫規章**: 後台動態調整遊戲參數 (金幣、體力、Logo)。
- **操作紀錄**: Audit Logs 完整記錄後台操作。
- **體力機制**: 自動排程恢復體力 (每 15 分鐘)。

### 5. 開發工具
- **一鍵啟動**: 內建 `dev.bat` (Windows)，可同時啟動 Server, Vite 與排程器。

## 🚀 安裝與執行

1. **環境建置**
   ```bash
   composer install
   npm install
   npm run build
   ```

2. **資料庫與語系設定**
   ```bash
   cp .env.example .env
   php artisan key:generate
   # 設定 .env 資料庫連線，並確保 APP_LOCALE=zh_TW
   
   # 安裝中文語系包
   composer require laravel-lang/common --dev
   php artisan lang:add zh_TW
   
   # 執行遷移與種子資料
   php artisan migrate --seed
   ```

3. **匯入頭像 (Avatar Seeder)**
   *請先將頭像圖片放入 `public/images/avatar/` 資料夾中*
   ```bash
   php artisan db:seed --class=AvatarSeeder
   ```

4. **建立檔案連結**
   ```bash
   php artisan storage:link
   ```

5. **啟動開發環境**
   * **Windows**: 直接雙擊執行 `dev.bat`
   * **Manual**:
       ```bash
       php artisan serve
       php artisan schedule:work
       npm run dev
       ```

## 📝 預設帳號
- **運營總督 (OM)**: `service@gkgary.com` / `game@1234`
- **測試玩家**: `player@example.com` / `game@1234`